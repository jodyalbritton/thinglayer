
<div id="thing_<%= humidity.id %>" class="tile col-lg-2 col-md-3 col-sm-6 col-xs-6  zone_<%= humidity.zone_id %> item">
  <div class="dummy"></div>
    <div class="thumbnail">
        <div class="widget-title handle">
        <h4 class="hidden-xs" ><%= humidity.zone.name %></h4>

        </div>
        <div class="temp humidity_<%= humidity.uid %> widget-profile-avatar">
          <%= humidity.device_value %> %
        </div>
         <h5><%= link_to humidity.label, thing_path(humidity) %></h5>
    </div>
</div>