
<div id="thing_<%= contact.id %>" class="tile col-lg-2 col-md-3 col-sm-6 col-xs-6  zone_<%= contact.zone_id %> item">
  <div class="dummy"></div>
      <div class="thumbnail">
          <div class="widget-title handle">
          <h4 class="hidden-xs"><%= contact.zone.name %></h4>

          </div>
          <div class="temp contact_<%= contact.uid %> img-circle">
            <%= contact.device_value %>
          </div>
          <h5><%= link_to contact.label, thing_path(contact) %></h5>
      </div>
</div>